# config

Environment-driven configuration loading (see `.env.example` in the repo
root for the full list of variables). Responsible for validating and
exposing `TRADING_MODE`, and for asserting that live mode cannot be
enabled until it is explicitly implemented and reviewed.

Status: not implemented.
